# 07 - Final Closure Audit

This audit re-reads `00-review.md` through `06-planner-scope.md` after remediation Plans 1-7. Statuses are based on the current branch state, not on intent.

| Source | Finding | Status | Evidence | Follow-up |
|---|---|---|---|---|
| 00-review.md, 02-review.md, 06-planner-scope.md | Builder invalid current draft could be treated as empty and overwritten by autosave. | solved | `src/features/builder/builder-persistence.ts` returns `LoadBuilderDraftResult` with `invalid-current`; `src/features/builder/useBuilderViewModel.ts` suppresses autosave when current storage is invalid; `src/features/builder/builder-persistence.test.ts` and `src/features/builder/useBuilderViewModel.test.ts` cover invalid current and legacy migration write failure. | none |
| 02-review.md, 06-planner-scope.md | Current collection persistence v2 parsing was lenient and could normalize malformed/current legacy-ID payloads. | solved | `src/features/collection/collectionMigrations.ts` separates `parsePublicCollectionSnapshot` from `migrateLegacyCollectionSnapshot` and requires public IDs plus required maps for current v2; `src/features/collection/collectionMigrations.test.ts` covers malformed current payloads, legacy migration, and strict public IDs. | none |
| 02-review.md, 06-planner-scope.md | Collection load/store invalid-current policies differed. | solved | `loadCollectionOwnership` now returns `ParseCollectionOwnershipSnapshotResult` instead of defaulting invalid current snapshots; `src/stores/collectionOwnershipStore.ts` blocks on invalid current; tests cover the shared result shape. | none |
| 02-review.md, 06-planner-scope.md | Collection import reported parse success even when persistence failed. | solved | `src/stores/collectionOwnershipStore.ts` returns persisted success or save failure from `importSnapshot`; `src/stores/collectionOwnershipStore.test.ts` covers failed persistence visibility. | none |
| 00-review.md, 02-review.md | Legacy/current collection snapshot responsibilities were mixed. | solved | Current parsing and serialization are public-v2 snapshot functions; legacy migration is isolated in `migrateLegacyCollectionSnapshot` inside `src/features/collection/collectionMigrations.ts`. | none |
| 04-review.md, 06-planner-scope.md | Local verify, CI, and pre-commit had different authorities; verify mutated formatting. | solved | `package.json` uses non-mutating `verify`: `format:check`, lint, bounded tests, script tests, build; `.github/workflows/ci.yml` runs `npm run verify`; `scripts/pre-commit-verify.mjs` runs staged format check plus the same substantive gates. | none |
| 04-review.md | Node script tests were outside standard verification. | solved | `package.json` defines `test:scripts` and includes it in `verify`; `scripts/pre-commit-verify.mjs` runs `test:scripts`. | none |
| 04-review.md | App TypeScript config exposed Vitest globals to production source. | solved | `tsconfig.app.json` contains only `vite/client` and excludes test files/setup. | none |
| 04-review.md, 05-wrap-up.md | Generated public-v3 data was formatted by the website without a producer-owned boundary. | solved | `.prettierignore` excludes `src/data/public-v3/**/*.json`; generated metadata/export integrity is checked from `C:\Users\dansa\CascadeProjects\MomenTB-Tools` with the public-v3 export check. | none |
| 04-review.md | Pre-commit formatting operated on broad dirty worktree state. | solved | `scripts/pre-commit-verify.mjs` calls `scripts/format-changed-files.mjs --staged --quiet --fail-on-write`; broad changed-file formatting remains an explicit manual command. | none |
| 04-review.md | Destructive WebP conversion had no first-class script-test gate. | solved | `scripts/convert-png-assets-to-webp.test.mjs` is now part of `npm run verify` through `test:scripts`. | Future conversion redesign can improve atomicity if the script is reused for another migration, but this branch no longer has an untested destructive script path. |
| 01-review.md, 06-planner-scope.md | Repositories accepted broad public-data scopes and unsupported search/snapshot scopes silently returned empty success. | solved | `src/data-access/public-data/scopeRegistry.ts` exposes capability descriptors/types; `searchRepository.ts` and `recordSnapshots.ts` assert capabilities; `repository.test.ts` covers unsupported search and snapshot capability invariants. | none |
| 01-review.md, 06-planner-scope.md | Public-data schemas parsed shape but did not verify requested scope/kind/id consistency. | solved | `scopeRegistry.ts` enforces scope/kind/id-prefix invariants; catalog, record, search, route, and snapshot tests in `repository.test.ts` assert generated data alignment. | none |
| 01-review.md, 05-wrap-up.md, 06-planner-scope.md | Website code remembered generated facts such as scope metadata and promoted derived IDs. | solved | Scope descriptors now come from `src/data/public-v3/indexes/scopes.json`; promoted derived exceptions are isolated in `src/domain/legacy-public-v3-adapter/compatibilityOverrides.ts`; `src/domain/public-data-runtime-boundary.test.ts` guards the compatibility override boundary. | Keep deleting the legacy adapter once old consumers are removed. |
| 00-review.md, 01-review.md, 06-planner-scope.md | Public description args had parallel public and legacy runtime schemas. | solved | `src/domain/public-description-args.schema.ts` is the public schema/type source; public detail adapters and full public record consumers (`wheels-full.ts`, `covenants-full.ts`, `posses-full.ts`, `relics.ts`) validate public-v3 records with `publicDescriptionArgsSchema`. | none |
| 00-review.md, 01-review.md, 06-planner-scope.md | Public detail adaptation was an oversized transitional boundary. | partially solved | Generated scope facts and public description schemas have been pulled out; compatibility-only promoted overrides are isolated under `legacy-public-v3-adapter`; public loaders remain in `src/domain/public-detail-record-adapters.ts` because legacy UI/domain consumers still depend on them. | Deferred as a documented migration cleanup, not a Goal 8 blocker; delete or split the facade when legacy detail consumers are replaced. |
| 00-review.md, 01-review.md, 02-review.md, 06-planner-scope.md | Public ID validators were duplicated/inconsistent. | solved | Public ID validation routes through `src/data-access/public-data/ids.ts` and scope descriptors; collection migration delegates public ID checks to `isPublicEntityId`; public-data repository tests assert id prefixes by descriptor. | Full branded ID types remain a later polish option. |
| 00-review.md, 01-review.md, 06-planner-scope.md | Search fallback caching ignored fallback field options; generated tokens were unused. | solved | `src/domain/public-search.ts` no longer caches indexes by entity array/options and merges generated `document.tokens` into the searchable `token` field; search tests cover fallback behavior. | none |
| 01-review.md, 06-planner-scope.md | Reference ambiguity was erased as absence. | solved | `src/data-access/public-data/referenceRepository.ts` exposes `resolvePublicReferenceTokenResult` with `match`, `ambiguous`, and `notFound`; `repository.test.ts` covers an ambiguous generated token. | Existing `resolvePublicReferenceToken` remains a compatibility convenience returning refs only. |
| 02-review.md, 06-planner-scope.md | Standard-code awakener decode used legacy names instead of public IDs first. | solved | `src/domain/import-export.ts` resolves standard-code awakener indexes through `standardAwakenerIdByIndex` and `awakenerById` before legacy-name fallback; import/export tests cover public-ID decode behavior. | none |
| 02-review.md, 06-planner-scope.md | In-game token ambiguity was modeled but surfaced as unknown or first match. | solved | `src/domain/ingame-codec.ts` emits `ambiguous_parse` with candidate IDs; `src/domain/ingame-codec.test.ts` and `src/features/builder/BuilderPage.import-export.test.tsx` cover surfaced ambiguity warnings. | none |
| 03-review.md, 06-planner-scope.md | `SearchCombobox` could crash when results shrank with stale `activeIndex`. | solved | `src/ui/search/SearchCombobox.tsx` clamps the active result internally; `src/ui/search/SearchCombobox.test.tsx` covers shrunk-result behavior. | none |
| 00-review.md, 02-review.md, 03-review.md, 06-planner-scope.md | Collection ownership mutation logic and remembered levels had two owners/shared mutable defaults. | solved | `collectionOwnershipStore.ts` is now a persistence-backed state store; `useCollectionViewModel.ts` owns UI remembered levels via `createInitialRememberedLevels`; tests cover independent containers and store behavior. | none |
| 03-review.md, 06-planner-scope.md | Toggling posses marked wheel sort pending. | solved | `useCollectionViewModel.ts` marks wheel pending only for wheel ownership changes; `useCollectionViewModel.test.ts` covers posse toggles. | none |
| 03-review.md, 06-planner-scope.md | Collection global search capture stayed active behind detail overlays. | solved | `CollectionPage.tsx` disables `useGlobalSearchCapture` while non-route detail overlay entries are open; `CollectionPage.test.tsx` covers typing while a detail overlay is open. | none |
| 00-review.md, 03-review.md, 06-planner-scope.md | Database popover hydration could mutate stale trails after async completion. | solved | `useDatabasePopoverController.ts` uses root request invalidation and source-entry checks before applying hydration; `useDatabasePopoverController.test.tsx` covers stale root hydration. | none |
| 03-review.md, 06-planner-scope.md | Detail navigation callbacks used names where IDs should be preserved. | solved | Detail callbacks now accept `{id, name}` where available; `DbDetailModalHost.tsx`, `DatabasePage.tsx`, and `CollectionPage.tsx` resolve by ID first; affected modal/popover tests assert ID-preserving payloads. | none |
| 00-review.md, 03-review.md, 06-planner-scope.md | Builder command ownership is split across store, view-model, page, and action factories. | deferred: documented non-goal | No concrete behavior bug was proven during Plan 7; `docs/notes/2026-05-08-builder-command-ownership-follow-up.md` records the deferral and scope rule. | Track only with a reproduced failing behavior or a planned builder command-layer refactor. |
| 03-review.md, 05-wrap-up.md | Database route descriptor registry, modal chrome/lifecycle split, entity browse registry cleanup, and `CompactArtTile` slot refactor. | deferred: documented non-goal | These were cleanup/architecture pressure items, not verified production-safety bugs in the final scope. Goal 7 fixed the concrete route/detail identity and overlay safety issues without broad UI rewrites. | Revisit as normal refactors after public-v3 migration lands. |
| 01-review.md, 05-wrap-up.md | Catalogs still act as summaries and awakener-lite data. | deferred: documented non-goal | Generator output and current consumers intentionally still use catalog-enriched summaries; repository integrity tests now validate scope/kind/id boundaries, reducing the risk that motivated the finding. | Consider a generated `entity-summaries` index if catalog shape becomes confusing again. |
| 00-review.md, 02-review.md, 05-wrap-up.md | Handwritten persistence migration data remains in domain code. | partially solved | Runtime safety is fixed and tests now protect invalid-current/current-vs-legacy behavior; the historical migration tables remain in `src/domain/persistence-id-migration.ts` as frozen compatibility data. | Future cleanup can move tables to generated/frozen data artifacts once v1 migration support is ready to retire. |
| 04-review.md, 05-wrap-up.md | Browser smoke was described in historical docs but not automated. | deferred: documented non-goal | Final Goal 8 includes a manual local smoke of collection overlay search, database detail/nested refs, builder persistence/import-export, and collection import/export/reload; CI still relies on unit/integration/build gates. | Add Playwright smoke only if the team wants CI browser coverage beyond this branch. |
| 06-planner-scope.md | Public/data V2 terminology might survive as a compatibility target. | solved | Review/plan wording now distinguishes `public-v3`, `legacy V1`, and `persistence v2`; source guard tests focus on runtime boundary and legacy compatibility naming. | Historical docs remain untracked and should not be treated as executable plans. |

## Final Result

- Verification run: `npm run verify` passed (`format:check`, lint, 162 Vitest files / 1040 tests, 4 Node script tests, production build).
- Tooling public-v3 checks: `uv run momentb-tools export-public-v3-website --website-root C:\Users\dansa\CascadeProjects\MomenTB --check` passed with `drift: 0`; the Windows-expanded public-v3 pytest set passed (65 tests). The literal plan glob `tests/test_public_v3_*.py` did not expand under PowerShell, so the matching files were run explicitly.
- Browser smoke: local Vite on `http://127.0.0.1:5174/`; collection detail overlay blocked underlying keyboard search, database detail routes opened for awakener and wheel records, builder invalid-current persistence showed the blocked safety state, and collection reloaded with a valid persistence v2 snapshot.
- Remaining residual risks: legacy detail adapter and frozen migration tables remain transitional by design, but are isolated or documented.
- Deferred non-goals: broad builder command rewrite, route descriptor registry, modal chrome split, browse registry cleanup, card slot refactor, automated Playwright smoke.
- Follow-up issues worth tracking later: delete `legacy-public-v3-adapter` once legacy detail consumers are gone; move frozen persistence migration tables to generated/frozen artifacts when v1 support can be retired.
