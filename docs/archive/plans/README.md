# Archived Plans

This folder is for shipped or superseded implementation plans that are still safe and useful to keep in the tracked repo.

- Current active work belongs in `../../plans/`.
- Historical reasoning that is no longer an active execution plan belongs here.
- If a historical plan is removed during a sanitization pass, prefer updating links to the archive index instead of leaving dead file references behind.
